import ContactsDisplay from "../features/contacts/ContactsDisplay";
import { Box } from "@mui/material";

const Contacts = () => {
    return (
        <ContactsDisplay />
    )
}

export default Contacts